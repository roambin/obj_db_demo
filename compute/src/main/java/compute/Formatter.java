package compute;

import compute.entity.OperatorResult;
import compute.entity.Result;

public class Formatter {
    public Result format(OperatorResult operatorResult){
        Result result = new Result();
        return result;
    }
}
