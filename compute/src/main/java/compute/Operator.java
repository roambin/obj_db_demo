package compute;

import compute.entity.LogicalPlan;
import compute.entity.OperatorResult;

public class Operator {
    public OperatorResult operate(LogicalPlan logicalPlan) {
        OperatorResult operatorResult = new OperatorResult();
        return operatorResult;
    }
}
